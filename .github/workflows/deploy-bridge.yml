name: OKEY17 deploy bridge

# DEVRE DISI — 2026-08-21
# Bu koprü Termux doneminden kalma. Saat basi tetiklenip bootstrap.cjs,
# gobek17-app.zip ve package.json dosyalarini eski bir surumle geri yaziyordu
# (commit 3a5268c: v171 -> v169). Tek release otoritesi artik Cloud Agent.
#
# Zamanlanmis tetikleme KALDIRILDI. Yalnizca Actions ekranindan elle
# calistirilabilir. Gecmis kayit olarak durur.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Devre disi
        run: |
          echo "deploy-bridge devre disi. Release akisi: G17 Cloud Agent."
          echo "https://g17-cloud-agent-production.up.railway.app"
